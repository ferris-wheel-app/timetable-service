akka {
  loglevel = DEBUG
}

http {
  interface = "0.0.0.0"
  port = 9000
}

db = {
  driver = com.mysql.jdbc.Driver
  url = "jdbc:mysql://localhost:3306/timetable"
  user = "root"
}

cors.allowed-origin = "*"

planning-service {
  acceptable-progress = 80
}
